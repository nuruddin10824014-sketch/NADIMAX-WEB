<div class="stats-card fade-in">

    <div class="stats-info">

        <h4>{{ $title }}</h4>

        <h2>{{ $value }}</h2>

        <span>{{ $subtitle }}</span>

    </div>

    <div class="stats-icon {{ $color }}">

        <i class="{{ $icon }}"></i>

    </div>

</div>